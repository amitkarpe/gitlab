# Run sarek with internet

### Background

* Need to demonstrate AWS setup with gitlab (for private pipleline code) and AWS ECR or any docker repository (for private container image)
* This folder should have commands / instructions to download or basic setup (like install nexflow binary and docker image)


### Expected results
* provide shell scripts to run sarek nexflow with binary 
* provide shell scripts to run sarek nexflow with docker image
* provide shell scripts to run sarek nexflow with binary with parameter
* provide shell scripts to run sarek nexflow with docker image with parameter
* provide shell scripts to run sarek nexflow with binary with test profile
* provide shell scripts to run sarek nexflow with docker image with test profile
* provide shell scripts to run sarek nexflow with binary  with docker profile
* provide shell scripts to run sarek nexflow with docker image with docker profile

### Constraints
* Every script must have explaination about its execution inputs and expected outputs
* Demo or execution is not about understanding of Nextflow or Sarek but its about providing infrastrcture support 
* EC2 intance is running ="Amazon Linux 2023.5.20240624"
* Lets document all tools/ packages required to run this demo
* Consider this is template where we have to run and demo multiple such nextflow pipeline to Data Researchers from  bioinformatic data analysis.