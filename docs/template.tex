% PANDOC LaTeX Template - Simplified Tables for Compatibility
\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$]{$if(documentclass)$$documentclass$$else$article$endif$}

% --- Essential Packages ---
\usepackage{fontenc}
% \usepackage[utf8]{inputenc}
% \usepackage[T1]{fontenc}
\usepackage{framed}
\usepackage{geometry}
% Using hardcoded geometry as per previous step to bypass YAML issues
\geometry{top=1in, bottom=1in, left=1in, right=1in}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{longtable, booktabs} % For Pandoc's default table output
\usepackage{grffile}
\usepackage[strings]{underscore} % Treat underscores literally
\usepackage[table]{xcolor}    % Keep for potential manual coloring, though auto-color removed
\usepackage{colortbl}         % Keep for potential manual coloring
\usepackage{array}            % Keep for basic table functions

% Load hyperref AFTER underscore package
\usepackage{hyperref}

% --- Define Table Colors (kept in case needed manually later) ---
\definecolor{lightgray}{gray}{0.90}

% --- REMOVED \\renewenvironment{longtable} block ---
% Table styling will now rely on Pandoc defaults + booktabs

% --- Add definition for Pandoc's \tightlist command ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% --- Basic Hyperlink Setup ---
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    urlcolor=cyan,
    pdftitle={$title$},
    pdfauthor={$author$},
    breaklinks=true,
    pdfencoding=auto
}

% --- Header and Footer Setup ---
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

$if(header-text)$
  \fancyhead[L]{$header-text$}
$endif$
$if(logo-path)$
  \fancyhead[R]{\includegraphics[height=1cm]{$logo-path$}}
$endif$
$if(footer-text)$
  \fancyfoot[C]{\small $footer-text$}
$endif$
\fancyfoot[R]{\thepage}

\fancypagestyle{plain}{%
  \fancyhf{}
  $if(footer-text)$
    \fancyfoot[C]{\small $footer-text$}
  $endif$
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
}

% --- Document Metadata ---
$if(title)$
  \begin{center} % Center the metadata
    \title{$title$$if(subtitle)$\\[1ex]\large $subtitle$$endif$}
    \author{$author$}
    $if(date)$
      \date{$date$}
    $endif$
  \end{center}
  \maketitle
  \newpage % Add a page break after the title page
  $if(toc)$ % Check if TOC is requested *before* inserting the page break
  \newpage   % <<< Add this line to force a new page AFTER the title page
  $endif$
$endif$

$if(toc)$
  {
  \hypersetup{linkcolor=black}
  \setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
  \tableofcontents
  \newpage
  }
$endif$

$if(number-sections)$
  \setcounter{secnumdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
$endif$

% --- Main Content ---
$body$

% --- Document End ---
\end{document}
